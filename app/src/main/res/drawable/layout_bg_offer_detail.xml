<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:topLeftRadius="70dp" />

    <!-- Fondo con el color definido -->
    <solid android:color="@color/secondaryBackgroundFound" />

    <!-- Borde gris con ancho de 1dp -->
    <stroke
        android:width="1dp"
        android:color="@color/borderBackgroundFound" />

</shape>
