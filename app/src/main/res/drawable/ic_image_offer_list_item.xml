<?xml version="1.0" encoding="utf-8"?>


<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="@dimen/radius" /> <!-- El valor en "radius" define la redondez -->
    <stroke
        android:width="1dp"
        android:color="@android:color/transparent" /> <!-- Opcional: puedes definir un color para el borde -->
</shape>

    <!--<shape xmlns:android="http://schemas.android.com/apk/res/android"
        android:shape="oval">
        <solid android:color="@color/gris_clarisimo"/>
        <corners android:radius="8dp"/>
        <stroke android:width="1dp" android:color="@color/gris_tenue"/>
    </shape>-->