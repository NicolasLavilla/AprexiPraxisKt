<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/offer_detail_navigation"
    app:startDestination="@id/license_detail_fragment">

    <fragment
        android:id="@+id/license_detail_fragment"
        android:name="com.aprexi.praxis.myapplication.presentation.fragment.LicenseDetailFragment"
        tools:layout="@layout/fragment_license_detail">

        <argument
            android:name="idLicenseUser"
            app:argType="integer" />
        <argument
            android:name="idLicense"
            app:argType="integer" />

        <argument
            android:name="idFragment"
            app:argType="integer" />

    </fragment>
</navigation>


